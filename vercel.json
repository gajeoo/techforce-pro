{
  "buildCommand": "pnpm --filter @workspace/techforce-pro run build",
  "outputDirectory": "artifacts/techforce-pro/dist/public",
  "installCommand": "npm install -g pnpm && pnpm install --frozen-lockfile",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
